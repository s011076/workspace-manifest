# The Cost of Silence —反面教材包

**一句話：這就是沉默的代價。**

同一個任務、同樣的 24 頁、同一個 agent 能力。唯一區別：有沒有守恆式 + typed_reason。

## 跑它（5 秒）

```bash
python3 compare.py
```

## 文件

| 文件 | 內容 |
|---|---|
| `01-without-manifest.log` | 傳統方式：聊天訊息交付，靜默失敗全紀錄 |
| `02-with-manifest-receipt.json` | 同一交付的 workspace-manifest receipt |
| `compare.py` | 並排對比兩者 |

## 給誰看

給還在用「Done! 👍」式匯報的團隊。先讓他們痛，再給解方。
